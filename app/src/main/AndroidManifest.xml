<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.podiatry.podiatrylogin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PodiatryLogin"
        android:usesCleartextTraffic="true">
        <activity android:name=".controlPanel.MostRankedActivity"></activity>
        <activity android:name=".controlPanel.AllProductsActivity" />
        <activity android:name=".controlPanel.ControlPanelActivity">
            android:name="..ControlPanelActivity"
            android:label="@string/title_activity_control_panel"
            android:theme="@style/Theme.PodiatryLogin.NoActionBar" />
        </activity>
        <!--
activity
            android:name="..ControlPanelActivity"
            android:label="@string/title_activity_control_panel"
            android:theme="@style/Theme.PodiatryLogin.NoActionBar" /
        -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>